import java.util.ArrayList;
import java.util.HashMap;
import java.util.List;

public class test {
//    public static HashMap<Integer, Boolean> map = new HashMap<Integer, Boolean>();
/*
    public static int getDistance(int from, int toID) {
        int to = toID % 2 == 0 ? toID / 2 : toID / 2 + 1;
        int processTime = toID % 2 == 0 ? myArgs.get(TimeToPutForEven) : myArgs.get(TimeToPutForOdd);
        if (from == to)
            // 原位置不动
            return processTime
        else if (Math.abs(from - to) == 1)
            // 挪动一步
            return processTime + myArgs.get(TimeToMoveOne);
        else if (Math.abs(from - to) == 2)
            // 挪动两步
            return processTime + myArgs.get(TimeToMoveTwo);
        else
            // 挪动三步
            return processTime + myArgs.get(TimeToMoveThree);
    }

    public static int toNext(int ID, HashMap map) {
        ArrayList<Integer> list = new ArrayList<>();
        for (Object key : map.keySet()) {
            if ((boolean) map.get(key)) {
                list.add((int) key);
            }
        }
        int toID = 0;        // 要前往的id
        int time = 3600; // 初始时间
        for (int i : list) {
            if (getDistance(ID, i) < time) {
                time = getDistance(ID, i);
                toID = i;
            }
        }
        return toID;
    }
*/
//    public static void main(String[] args) {
//        map.put(1, true);
//        map.put(2, true);
//        map.put(4, true);
////        toNext(1, map);
//        System.out.println(toNext(1, map));
//    }
}
